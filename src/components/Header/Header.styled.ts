
import { styled } from "styled-components";

const HeaderContainer = styled.header`
  width: 100%;

`;

const NavMenu = styled.nav`
  display: flex;
  gap: 30px;
`;

export const HeaderStyled = { HeaderContainer, NavMenu };
