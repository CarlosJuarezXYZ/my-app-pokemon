export enum PokemonType {
    fire = "fire",
    water = "water",
    electric = "electric",
    dragon = "dragon",
    ghost = "ghost",
}

export interface PokemonInterface {
    name:string;
    image:string;
};
